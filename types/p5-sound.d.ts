declare module "p5/lib/addons/p5.sound" {
  // Minimal declaration so TypeScript accepts the import. Runtime types
  // come from the main @types/p5 package.
  const p5Sound: unknown;
  export default p5Sound;
}
